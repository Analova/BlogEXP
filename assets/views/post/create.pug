extends ../layout/app
block seo 
   title Homepage 

block content  
   // Banner
   section#banner
   .content
     form(action="/post/create" method="POST")
      lable Title
      input(type="text" name="title"
      id="demo-name" value="" placeholder="Name")
      lable Description
      textarea(name="description"
      id="demo-message"  placeholder="Enter your message" rows="6")
      input(type="submit" value="Send Message"
      class="primary" )